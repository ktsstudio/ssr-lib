{
  "name": "@kts/ssr-utils",
  "version": "1.0.0",
  "description": "",
  "main": "build/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lib:dev": "rimraf build && mkdir -p build && tsc --project tsconfig.json --watch"
  },
  "peerDependencies": {
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-router": "^5.2.0",
    "react-router-dom": "^5.2.0"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react-router-config": "^5.1.1",
    "react-topbar-progress-indicator": "^4.1.0"
  },
  "devDependencies": {
    "@types/react-router-config": "^5.0.2"
  }
}
